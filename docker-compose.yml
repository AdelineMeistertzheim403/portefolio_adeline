services:
  portfolio:
    build: .
    container_name: portfolio-adeline
    restart: unless-stopped
    labels:
      - traefik.enable=true
      - traefik.http.routers.portfolio-http.rule=Host(`adelinemeistertzheim.fr`) || Host(`www.adelinemeistertzheim.fr`)
      - traefik.http.routers.portfolio-http.entrypoints=web
      - traefik.http.routers.portfolio-http.middlewares=portfolio-https
      - traefik.http.middlewares.portfolio-https.redirectscheme.scheme=https
      - traefik.http.middlewares.portfolio-https.redirectscheme.permanent=true
      - traefik.http.routers.portfolio.rule=Host(`adelinemeistertzheim.fr`) || Host(`www.adelinemeistertzheim.fr`)
      - traefik.http.routers.portfolio.entrypoints=websecure
      - traefik.http.routers.portfolio.tls=true
      - traefik.http.routers.portfolio.tls.certresolver=letsencrypt
      - traefik.http.routers.portfolio.middlewares=portfolio-security
      - traefik.http.middlewares.portfolio-security.headers.stsSeconds=31536000
      - traefik.http.middlewares.portfolio-security.headers.stsIncludeSubdomains=true
      - traefik.http.middlewares.portfolio-security.headers.stsPreload=true
      - traefik.http.middlewares.portfolio-security.headers.browserXssFilter=true
      - traefik.http.middlewares.portfolio-security.headers.contentTypeNosniff=true
      - traefik.http.middlewares.portfolio-security.headers.frameDeny=true
      - traefik.http.services.portfolio.loadbalancer.server.port=80
      - traefik.docker.network=web
    networks:
      - web

networks:
  web:
    external: true
